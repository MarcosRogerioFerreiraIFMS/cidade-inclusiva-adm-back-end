# ===========================================
# CONFIGURAÇÕES DA APLICAÇÃO CIDADE INCLUSIVA
# ===========================================
# Copie este arquivo para .env e configure as variáveis abaixo

# Porta para o servidor (padrão: 5555)
PORT=5555

# ===========================================
# CONFIGURAÇÕES JWT (CRÍTICO)
# ===========================================
# ATENÇÃO: Configure essas variáveis antes de iniciar a aplicação!

# Chave secreta para JWT (OBRIGATÓRIO)
# Execute: pnpm generate-jwt-secret
# Copie uma das chaves e cole no .env
JWT_SECRET=

# Duração do token JWT
# Formatos válidos: 30s, 15m, 1h, 7d
# Recomendado: 1h para produção, 7d para desenvolvimento
JWT_EXPIRES_IN=7d

# ===========================================
# CONFIGURAÇÕES DO AMBIENTE
# ===========================================

# Ambiente de execução
# Valores: development | production | test
NODE_ENV=development

# ===========================================
# CONFIGURAÇÕES CORS
# ===========================================

# Origins permitidas (separadas por vírgula)
# Para desenvolvimento, inclua as URLs dos frontends
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# ===========================================
# CONFIGURAÇÕES DO BANCO DE DADOS
# ===========================================

# URL de conexão com o banco (SQLite por padrão)
# Para outros bancos, ajuste conforme necessário
DATABASE_URL="file:./dev.db"
