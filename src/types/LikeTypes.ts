import type { Like } from '@prisma/client'

/**
 * Tipo completo do like
 * Representa um like com todos os seus campos
 * Baseado na entidade Like do Prisma sem relacionamentos adicionais
 */
export type LikeCompletions = Like
